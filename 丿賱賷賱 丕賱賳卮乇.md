# دليل نشر الموقع (بدون أي برمجة)

اتبعي الخطوات بالترتيب. كل خطوة عبارة عن نقر وتعبئة، بدون أي كود.

---

## الخطوة 1: إنشاء قاعدة بيانات مجانية (Supabase)

هذا هو المكان الذي تُحفظ فيه المنتجات والطلبات بحيث تظهر لكل الزوار ولوحة التحكم.

1. ادخلي على https://supabase.com واضغطي "Start your project" وسجّلي بحساب Google أو بريدك.
2. اضغطي "New Project". اختاري اسم أي شيء، وكلمة مرور لقاعدة البيانات (احفظيها في مكان آمن)، والمنطقة الأقرب لكم.
3. بعد ما يجهز المشروع (تأخذ دقيقة أو دقيقتين)، من القائمة الجانبية اضغطي على "SQL Editor".
4. اضغطي "New query" والصقي هذا الكود بالكامل، ثم اضغطي "Run":

```sql
create table if not exists kv_store (
  key text primary key,
  value text not null,
  updated_at timestamptz default now()
);

alter table kv_store enable row level security;

create policy "public read" on kv_store for select using (true);
create policy "public write" on kv_store for insert with check (true);
create policy "public update" on kv_store for update using (true);
```

5. من القائمة الجانبية اضغطي على أيقونة الترس "Project Settings" ثم "API".
6. انسخي القيمتين التاليتين وخليهم عندك (بنحتاجهم بالخطوة 3):
   - **Project URL**
   - **anon public key**

---

## الخطوة 2: رفع ملفات الموقع إلى GitHub

1. ادخلي على https://github.com وأنشئي حساب مجاني إذا ما عندك.
2. اضغطي "+" أعلى الصفحة ثم "New repository". اعطيه اسم مثل `matjar-store` واضغطي "Create repository".
3. بالصفحة اللي تفتح، اضغطي رابط "uploading an existing file".
4. اسحبي **كل** الملفات والمجلدات اللي أرسلتها لك (عدا مجلد node_modules إذا موجود) وأفلتيها في الصفحة، ثم اضغطي "Commit changes".

---

## الخطوة 3: النشر عبر Vercel (مجاني)

1. ادخلي على https://vercel.com وسجّلي دخول باستخدام حساب GitHub نفسه.
2. اضغطي "Add New..." ثم "Project".
3. اختاري المستودع (repository) اللي رفعتيه `matjar-store` واضغطي "Import".
4. قبل الضغط على Deploy، افتحي قسم "Environment Variables" وأضيفي متغيرين:
   - الاسم: `VITE_SUPABASE_URL` — القيمة: الرابط اللي نسختيه من Supabase
   - الاسم: `VITE_SUPABASE_ANON_KEY` — القيمة: المفتاح اللي نسختيه من Supabase
5. اضغطي "Deploy" وانتظري دقيقة تقريبًا.
6. بعد ما تخلص، بيعطيك رابط مباشر للموقع مثل `matjar-store.vercel.app` — هذا رابط موقعك الحقيقي وشغّال لأي شخص حول العالم.

---

## الخطوة 4 (اختياري): ربط دومين خاص

من داخل مشروعك في Vercel، اذهبي لـ "Settings" ثم "Domains"، واكتبي الدومين اللي اشتريتيه (من أي موقع مثل GoDaddy أو Namecheap)، وراح تعطيك تعليمات بسيطة لربطه — كلها نسخ ولصق بدون برمجة.

---

## ملاحظة مهمة عن الأمان

رمز الدخول للوحة التحكم (1234 حاليًا) هو حماية بسيطة فقط لمنع الزوار العاديين من فتح اللوحة بالصدفة — وليس حماية قوية بمستوى بنكي. أي شخص يعرف الرابط الصحيح ويحاول بجد يقدر يتجاوزه نظريًا. لمتجر صغير بين العائلة/المعارف هذا مقبول عادة، لكن إذا كبر المشروع ونقل مبالغ كبيرة، يُفضّل لاحقًا إضافة نظام دخول حقيقي (تسجيل دخول بكلمة مرور فعلية) — وهذا يحتاج مطوّر.

---

## تحديث بيانات المتجر لاحقًا

أي تعديل تسوّينه من داخل لوحة التحكم في الموقع نفسه (نشرته أو أضفته منتج، غيرتِ رقم الحوالة، إلخ) يُحفظ مباشرة في قاعدة البيانات — ما يحتاج تكرار خطوات النشر. خطوات النشر فوق تسوّينها مرة وحدة بس عند إطلاق الموقع لأول مرة.

إذا احتجتِ تعدّلين تصميم الموقع نفسه أو تضيفين مزايا جديدة مستقبلًا، ارجعي لي بالملفات وأساعدك أجهزها من جديد.
